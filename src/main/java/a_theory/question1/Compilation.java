package a_theory.question1;

public class Compilation {

    // Sometimes things get complicated, sometimes things get compiled.

    // In your words (do not use wiki definitions)
    // What is compilation?
    // Answer:
    // Compilation takes the code programmers write and turns it into binary, because most programmers can't
    // code in binary and computers only understand binary code. Code is compiled before the program is executed.

    // Name 2 benefits of compilation?
    // 1 once the application is compiled, it is ready to be executed by the user, which makes the user's life easier
    // 2 compiler also checks for errors in code

    // Name 1 disadvantage of compilation?
    // 1 large applications take lots of time to compile
}
