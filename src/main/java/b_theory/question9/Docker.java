package b_theory.question9;

public class Docker {

    //todo A
    // What is a server?
    // Answer: A server is a piece of computer hardware or software that provides functionality for other programs, that are called clients

    //todo B
    // What is the difference between build server and production server?
    // Answer: Usually the difference is in security. A build server allows unrestricted access and control to a group of users.
    // A production server is configuerd to restrict access to authorized users and limit control

    //todo C
    // What is docker?
    // Answer: Docker allows to package and run an application in an environment called a container

    //todo D
    // Name and explain docker container benefits over virtual machine setup (java jar as system process and installed nginx)
    // 1 Containers have reduced and simplified security updates
    // 2 Containers have reduced IT management resources

    //todo E
    // Name and explain docker container drawback over virtual machine setup (java jar as system process and installed nginx)
    // 1 VMs have better known security controls

    //todo F
    // Name and describe tools for docker architecture
    // 1 Kubernetes
    // 2 Google Container Engine

    //todo G
    // Name and explain why are companies slow in moving existing systems to docker architecture (do not explain why docker is bad)
    // 1 Companies may be slow in moving to docker because they have alot of valuable data to store
    // 2 Another times my be because they prioritize security
}
